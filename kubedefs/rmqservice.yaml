apiVersion: v1
kind: Service
metadata:
  name: vpromq01
spec:
  type: ClusterIP
  selector:
    app: vpromq
  ports:
    - port: 5672
      targetPort: vpromq-port
      protocol: TCP
      name: mysql
